# appspec.yml

version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/uni-sparkle-deploy/uni-sparkle
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: ec2-user